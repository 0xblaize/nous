# MOVED — env is now a single project-wide file at the repo root.
# See ../.env.example (copy the root .env.example to root .env).
# This backend reads Nous/.env via config.py. A backend-local .env still
# overrides it if you re-create one here for per-service tweaks.
